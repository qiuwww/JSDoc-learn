/** @function myFunction */
var myFunction = (pages) => {};
/**
 * 参数结构的注释
 * @param {Object} option - 传入的对象
 * @param {String} option.name - 参数name
 * @param {Number} option.age - 参数age
 */
function bar({ name, age }) {
    return { name, age }
}